cmake_minimum_required(VERSION 3.10)
project(h5p C)
include(CTest)
find_package(MPI REQUIRED)
find_package(ZLIB REQUIRED)
include_directories(${MPI_C_INCLUDE_DIRS})
add_executable(h5p src/b.c)
target_link_libraries(h5p ${MPI_C_LIBRARIES} ${ZLIB_LIBRARIES})
enable_testing()
add_test(no_option h5p)

